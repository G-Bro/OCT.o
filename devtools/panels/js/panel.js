

var app = new Vue({
  el: '#app',
  data: {
    message: 'Hello Vue!'
  }
})

// const port = browser.runtime.connect(null, { name: "octo-panel" });

// port.onMessage.addListener((message) => {
//   console.log(message);
// });